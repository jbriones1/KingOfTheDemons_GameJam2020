using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class Send_er : MonoBehaviour
{

    public int numPlayers = 1;


    // Start is called before the first frame update
    void Start()
    {
        numPlayers = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
